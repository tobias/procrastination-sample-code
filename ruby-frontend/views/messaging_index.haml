%h2 over sync messaging
%form{:method => 'get', :action => '/m'}
  %input{:type => 'text', :name => 'data', :value => params['data']}
  %br/
  %br/
  %input{:type => 'submit', :value => 'Haminize!'}

- if @result
  %h3 result:
  = @result.inspect
  %br/

- if @time
  == time: #{@time * 1000} ms
